services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - pokemon-network
    ports:
      - ${PORT}:${PORT}
    env_file:
      - .env
    command: ["python", "main.py"]

networks:
  pokemon-network:
    external: true
